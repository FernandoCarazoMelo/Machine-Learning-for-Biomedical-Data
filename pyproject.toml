[project]
name = "clase-python"
version = "0.1.0"
description = "Entorno para alumnos"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas",
    "matplotlib",
    "requests",
    "numpy",
    "matplotlib",
    "scikit-learn",
    "ipykernel"
]
